# The 16 MB waiver packet lives in Google Drive, not the repo.
# (The Apps Script attaches it from Drive by file ID.)
*.zip

# OS cruft
.DS_Store

# Cloudflare wrangler
cloudflare-worker/.wrangler/
node_modules/
